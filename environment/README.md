# Environment notes

- Primary dependency list: `requirements.txt` at repo root.
- Conda users: see `environment.yml` for a pinned base; adjust CUDA if needed.
- Docker users: see `Dockerfile` for a slim Python image installing the same requirements.
